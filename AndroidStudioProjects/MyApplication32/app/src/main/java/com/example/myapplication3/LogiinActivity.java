//..ckage com.example.myapplication3;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Toast;

//public class LogiinActivity extends AppCompatActivity {

    //EditText nu,mdp;
    //Button buttonLogin;

    //@Override
   // protected void onCreate(Bundle savedInstanceState) {
     //   super.onCreate(savedInstanceState);
       // setContentView(R.layout.activity_logiin);

        //nu = findViewById(R.id.nu);
        //mdp = findViewById(R.id.mdp);
        //buttonLogin = findViewById(R.id.buttonLogin);

        //buttonLogin.setOnClickListener(new View.OnClickListener() {
          //  @Override
           // public void onClick(View v) {
             //   String username = nu.getText().toString().trim();
               // String password = mdp.getText().toString().trim();

                // Vérifier si les champs username et password sont vides
                //if (username.isEmpty() || password.isEmpty()) {
                  //  Toast.makeText(LogiinActivity.this, "Veuillez remplir tous les champs", Toast.LENGTH_SHORT).show();
                    //return; // Arrêter l'exécution de la méthode onClick
                //}

                // Si les champs sont remplis, créer l'intent pour démarrer StudentsActivity
                //Intent intent = new Intent(LogiinActivity.this, MainActivity.class);
                //startActivity(intent);
                //finish();// Terminer l'activité actuelle
            //}
        //});
    //}
    //}